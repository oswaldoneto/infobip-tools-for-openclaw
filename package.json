{
  "name": "infobip",
  "version": "1.0.0",
  "description": "OpenClaw plugin for sending WhatsApp messages via Infobip",
  "type": "module",
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "keywords": ["openclaw", "infobip", "whatsapp"],
  "license": "MIT",
  "scripts": {
    "test": "node --test --import tsx --import dotenv/config",
    "test:integration": "node --test --import tsx --import dotenv/config test/integration.infobip.test.ts"
  }
}
